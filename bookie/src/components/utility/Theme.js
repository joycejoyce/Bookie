import { createMuiTheme } from '@material-ui/core/styles';

export const checkboxTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#EF476F'
        }
    }
});

export const rateTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#FEB610'
        }
    }
});